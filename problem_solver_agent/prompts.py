"""
prompts.py - Prompt 模板模块 (V5.0 — 模块级常量)

所有 Prompt 模板以模块级常量直接导出，模块本身就是命名空间。
导入方式：
    from problem_solver_agent import prompts
    print(prompts.CLASSIFICATION_PROMPT)
"""

# ==============================================================================
# 视觉模型提示词
# ==============================================================================

CLASSIFICATION_PROMPT = r"""
Analyze the content of the image(s). Your response MUST be ONLY ONE of the following keywords based on this priority order:

1.  **'MULTIPLE_CHOICE'**: If the problem consists of one or more questions, each followed by options (e.g., A, B, C, D). This classification takes the HIGHEST priority.
2.  **'FILL_IN_THE_BLANKS'**: If the problem is a fill-in-the-blanks question, often containing numbered placeholders, underscores, or text like "请输入答案". This takes second priority.
3.  **'ML_CODING'**: If the problem asks to implement / train / evaluate a machine-learning or deep-learning model (e.g. logistic regression, gradient descent, neural networks, and code using numpy / torch / tensorflow) AND it is NOT a multiple-choice or fill-in-the-blanks question. Judge by the problem statement itself, not by keyword matching.
4.  'CODING': If the problem is a programming challenge (like ACM/LeetCode) AND it is NOT any of the above.
5.  'VISUAL_REASONING': If the problem requires finding a pattern in shapes or figures, AND it is NOT a multiple-choice question.
6.  'QUESTION_ANSWERING': For tasks that ask a question about a given context, but are NOT fill-in-the-blanks or multiple-choice.
7.  'GENERAL': For any other text-based problem.
Respond with only the single, most appropriate keyword and nothing else.
"""

TRANSCRIPTION_PROMPT = r"""
你是一个世界顶级的、专门用于文档数字化的多模态识别引擎。你的任务是精确地识别单张图片中的所有内容，并将其转化为结构化的文本。
**核心要求：**
- **精确识别**: 识别图片中的所有文字、段落、列表、表格和数学公式。
- **结构保持**: 必须最大程度地保留原始文档的布局和格式。
- **纯净输出**: 你的输出只能包含识别出的内容，严禁添加任何前缀、后缀、解释或评论。
**格式化规则：**
1.  **表格 (Tables)**: 必须使用 **Markdown** 格式进行输出 (例如: `| Header 1 | Header 2 |\n|---|---|\n| Cell 1 | Cell 2 |`)。
2.  **数学公式 (Math Formulas)**: 必须使用 **LaTeX** 格式进行输出，行内公式使用 `$...$` 包裹，块级公式使用 `$$...$$` 包裹。
3.  **其他所有内容**: 严格按照原始的换行、缩进和排版进行输出。
现在，请处理你收到的**单张图片**，并严格按照上述规则，输出其包含的结构化文本。
"""

# ------------------------------------------------------------------------------
# 合并调用：一次请求同时完成"题型分类"与"逐页转录"
# 相比先分类、再逐页 OCR 的两轮调用，可省一轮网络往返与一次重复的图片计费。
#
# **协议为什么是分隔符而不是 JSON（2026-09-13 事故复盘）**：
# 旧协议把全部转录塞进一个 JSON 字符串，而 JSON 与 LaTeX 天然互斥——模型漏转义时
# `\frac`→`\f`(formfeed)、`\begin`→`\b`(backspace)、`\theta`→`\t`(tab)、`\neq`→`\n`
# 都是**合法转义**，解析"成功"但正文被静默破坏（`\frac{a}{b}` 变成 `␌rac{a}{b}`），
# 而且会通过页数与长度校验、直接写进解答文件，没有任何机制能发现。`\sqrt` 则会硬失败。
# 现在改成 `<<<PAGE n|NEW/CONT>>>` 分隔符：**正文逐字直出，不经过任何转义层**。
#
# 页边界用模型声明的序号（1 基）而不是出现顺序，因此跳号/重排能被发现并记为失败页；
# `CONT` 表示"与上一页是同一道题的延续，省略重复内容"，它顺带给出了版面判断
# （比单一 LAYOUT 字段更强：支持"前 3 页同一题、后 5 页独立题"的混合场景）。
# 解析失败时调用方按 `<<<PAGE>>>` → JSON 协议 → 并行路径 三级回退。
# ------------------------------------------------------------------------------
CLASSIFY_AND_TRANSCRIBE_PROMPT = r"""
你是一个多模态文档解析引擎。请同时完成两件事：题型分类 + 逐页转录。

**任务 A —— 题型分类**：从下列标签中选出唯一最合适的一个：
1. `MULTIPLE_CHOICE`：题目由一个或多个小题组成，每个小题后面跟着选项（A/B/C/D）。**优先级最高**。
2. `FILL_IN_THE_BLANKS`：填空题，通常含有编号占位符、下划线，或"请输入答案"之类的提示。**第二优先**。
3. `ML_CODING`：要求实现/训练/评估机器学习或深度学习模型（如逻辑回归、梯度下降、神经网络，代码涉及 numpy / torch / tensorflow 等），且不属于上面两类。按题面判断，不要只匹配关键词。
4. `CODING`：编程题（ACM / LeetCode 风格），且不属于上面三类。
5. `VISUAL_REASONING`：需要在图形、图案、规律中找规律的题，且不是选择题。
6. `QUESTION_ANSWERING`：针对给定上下文提问，且不属于填空或选择。
7. `GENERAL`：其他所有文字类题目。

**任务 B —— 逐页转录**：按**图片顺序**逐张转录，每张图一个页块。规则：
- 表格用 Markdown 表格语法输出。
- 数学公式用 LaTeX，行内用 `$...$`，块级用 `$$...$$`，`\begin{}`/`\end{}` 环境必须用 `$$...$$` 包裹。
- **LaTeX 反斜杠必须原样输出**（`\frac`、`\begin`、`\theta`、`\neq`、`\sqrt` 等），不要转成任何其他字符。
- 保持原文的换行与缩进；不要添加任何解释、评论或额外文字。
- 单张图片若无文字内容（例如纯图形题），页块正文留空即可。

**每张图必须且只能出现一个页块，顺序与图片顺序一致。** 判断相邻两页的关系并如实标注：
- 该页是一道新题的开头 → 标 `NEW`，完整转录该页全部内容。
- 该页是上一页那道题的延续（同一题翻页、内容接续）→ 标 `CONT`，**只输出与上一页不重复的部分**（重复的题干、选项不必再抄一遍）。
- **千万不要把两道不同的题当成同一个题的延续**：只要这一页出现了新的题号/新的题干开头，就是 `NEW`。
- 前面的客套话（"好的，我来转录"）和 Markdown 代码围栏一律不要输出。

**输出格式（必须严格遵守）**：
<<<TYPE>>>标签
<<<PAGE 1|NEW>>>
（第 1 张图的完整文本）
<<<PAGE 2|CONT>>>
（第 2 张图的文本；若与上一页是同一道题的延续，省略重复内容）
<<<PAGE 3|NEW>>>
（第 3 张图的完整文本）
<<<END>>>

除上述内容外不要输出任何其他文字。
"""


# JSON 协议版合并调用（**第二顺位回退**，仅为兼容保留）。
# 已知缺陷：LaTeX 反斜杠漏转义会静默损坏正文（见上方说明），因此不再作为首选协议。
CLASSIFY_AND_TRANSCRIBE_JSON_PROMPT = r"""
你是一个多模态文档解析引擎。请同时完成两件事：

**任务 A —— 题型分类**：从下列标签中选出唯一最合适的一个：
1. `MULTIPLE_CHOICE`：题目由一个或多个小题组成，每个小题后面跟着选项（A/B/C/D）。**优先级最高**。
2. `FILL_IN_THE_BLANKS`：填空题，通常含有编号占位符、下划线，或"请输入答案"之类的提示。**第二优先**。
3. `ML_CODING`：要求实现/训练/评估机器学习或深度学习模型，且不属于上面两类。
4. `CODING`：编程题（ACM / LeetCode 风格），且不属于上面三类。
5. `VISUAL_REASONING`：需要在图形、图案、规律中找规律的题，且不是选择题。
6. `QUESTION_ANSWERING`：针对给定上下文提问，且不属于填空或选择。
7. `GENERAL`：其他所有文字类题目。

**任务 B —— 逐页转录**：按顺序转录每一张图片的全部内容，输出一个数组，数组长度必须等于图片数量，顺序与图片顺序一致。
- 表格用 Markdown 表格语法输出。
- 数学公式用 LaTeX，行内用 `$...$`，块级用 `$$...$$`，`\begin{}`/`\end{}` 环境必须用 `$$...$$` 包裹。
- 保持原文的换行与缩进；不要添加任何解释、评论或额外文字。
- 单张图片若无文字内容（例如纯图形题），对应元素输出空字符串。

**输出格式（必须严格遵守）**：只输出一个 JSON 对象，不要输出 Markdown 代码块，不要输出任何其他文字。

{"problem_type": "<标签>", "pages": ["第一张图的文本", "第二张图的文本"]}
"""


# ==============================================================================
# 辅助模型提示词
# ==============================================================================

TEXT_MERGE_AND_POLISH_PROMPT = r"""
# 角色/任务
你是一位顶级的文档编辑专家。你的任务是接收多个由 '---[NEXT]---' 分隔的文本片段，并将它们智能地合并成一篇流畅、准确、格式正确的最终文档。

# 第一步（必须先做）：场景判断
先判断这些片段之间的关系属于下面哪一种，**再决定要不要去重**：
- **场景一：同一题的多页连续截图** —— 片段之间是同一道题的接续（翻页/滚动导致上下文重叠）。
  此时才允许做"重叠检测"：找到并丢弃重复的接缝内容，让过渡自然。
- **场景二：互不相关的多道题** —— 每个片段（或每几个片段）是各自独立的题目。
  **此时绝对禁止跨片段删除任何内容。** 相邻片段出现"下列哪项正确""（ ）"这类相同措辞
  不代表它们是同一道题；把它们当重叠删掉，会**静默删掉一整道题的开头**。
  场景二下你只做公式规范化与明显的 OCR 错字修正，正文一字不减。
- 判断不了时，**一律按场景二处理**（宁可不合并，不可丢内容）。

# 核心原则
1.  **内容保真**: 绝不臆测或添加原始文本中没有的信息；绝不因为"看起来像重复"就删除题目正文。
2.  **格式继承**: 严格保持所有Markdown表格的原始格式。
3.  **数学公式规范化**: **必须**将所有数学表达式用正确的 LaTeX 分隔符包裹。这是本任务最重要的要求。
   - 行内公式（短公式、变量、符号如 `X=1`、`b₁`、`E[X]`）：用 `$...$` 包裹，例如 `$X=1$`、`$E[X]=0.5$`。
   - 块级公式（矩阵、多行公式如 `\begin{bmatrix}...\end{bmatrix}`）：用 `$$...$$` 包裹并独占一行。
   - 所有 `\begin{...}` 到 `\end{...}` 的环境（如 matrix, bmatrix, align, cases 等）**必须**用 `$$...$$` 包裹。
   - 确保每个 `$` 或 `$$` 都成对出现，不要出现未闭合的数学分隔符。
   - LaTeX 反斜杠必须原样保留（`\frac` / `\begin` / `\theta` / `\neq` / `\sqrt`），不得改成任何其他字符。
4.  **无缝拼接**: 仅当第一步判定为**场景一**时，才识别并处理片段间的重叠内容，确保过渡自然。

# CoT (Chain of Thoughts) - 执行步骤
1.  **场景判断**: 先按上面的规则判定是"同一题多页"还是"多道独立题"。
2.  **顺序读取**: 依次分析每个由 '---[NEXT]---' 分隔的文本片段。
3.  **重叠检测（仅场景一）**: 比较当前片段的开头与前一片段的结尾，找到最长的重叠部分并丢弃。
    场景二下**跳过本步**，只做拼接。
4.  **合并与修正**: 拼接非重叠部分。在此过程中，修正明显的OCR识别错误（如 `hell0` -> `hello`）并修复不自然的断行。
5.  **公式规范化**: 遍历合并后的全文，确保所有数学表达式都已正确包裹。特别检查 `\begin`/`\end` 块、矩阵、分数、上下标等。
6.  **循环迭代**: 重复步骤3到5，直到所有片段处理完毕。

# 输出规范
- 你的输出必须是且只能是最终合并、润色、且数学公式已规范化的完整文本。
- 严禁包含任何解释、前缀、或元注释（如 "这是合并后的文本："）。

# 上下文处理
待处理的文本片段如下：
---
{raw_texts}
---
"""

FILENAME_GENERATION_PROMPT = r"""
# 角色/任务
你是一个专业的文件命名专家。你的任务是根据提供的文本内容，生成一个结构化的、信息丰富的文件名。

# 核心原则
1.  **信息优先**: 文件名必须首先反映题号信息，其次是核心主题。
2.  **格式固定**: 严格遵守"题号_主题"的格式。
3.  **简洁准确**: 主题概括必须高度凝练且切中要点。

# CoT (Chain of Thoughts) - 执行步骤
1.  **提取题号**: 仔细扫描文本，识别所有看起来像题目序号的数字。
2.  **格式化题号**:
    - 如果只有一个题号（如16），前缀就是 "16"。
    - 如果题号连续（如16, 17, 18），前缀就是 "16-18"。
    - 如果题号不连续（如1, 2, 5），前缀就是 "1,2,5"。
3.  **概括主题**: 用8到10个字，高度概括所有题目的核心主题。
4.  **拼接**: 将格式化后的题号和主题用下划线 `_` 连接。

# 输出规范
- 你的回复必须只包含最终拼接好的文件名字符串。
- 严禁添加任何前缀、后缀、引号或任何其他多余的文字。

# 示例 (Few-Shot Examples)
- **输入**: (包含16,17,18,19,20题，内容是多领域选择题) -> **输出**: `16-20_多领域选择题综合解答`
- **输入**: (只包含21题，内容是设备故障预测) -> **输出**: `21_设备故障预测程序`

# 上下文处理
待处理的文本内容如下:
---
{transcribed_text}
---
"""

# ------------------------------------------------------------------------------
# 文件名建议（T4）：挂到每一次求解调用上，把"隐形的文件名生成调用"并进求解首行。
# 此前每个任务都会额外调一次 deepseek 生成文件名，而且游离在 StageTimings 与
# UsageReport 之外 —— 用户既看不到它的耗时，也看不到它的费用。
# 由 `pipeline.build_prompt` 统一追加，`core_pipeline._run_solve_attempt` 负责剥掉首行。
# ------------------------------------------------------------------------------
FILENAME_SUGGESTION_INSTRUCTION = r"""

# 输出格式（必须遵守）
- 回答的**第一行**必须是文件名建议：`FILE: <题号>_<主题>`
  - 题号沿用题目里的原始编号：连续写 `16-20`，不连续写 `1,2,5`；无法确定题号时只写主题。
  - 主题用 8–10 个字概括所有题目。
  - 例：`FILE: 16-20_多领域选择题综合解答`
- **第二行起**照常输出解答正文（Markdown）。不要重复 FILE 行，也不要给它加代码围栏。
"""

# ==============================================================================
# 核心求解器 Prompt 模板
# ==============================================================================

_FILL_IN_THE_BLANKS = r"""
# 角色/任务
你是一位知识渊博的、精确的学科专家。你的任务是根据你的内部知识库，准确地回答下面的填空题。
# 核心原则
1.  **知识驱动**: 你必须使用你自己的知识来回答问题，而不是仅仅依赖于问题文本。
2.  **精准回答**: 答案应该是最标准、最简洁的专业术语或数值。
3.  **完整性**: 必须回答所有的空格，并提供简要的知识点解析。
# CoT (Chain of Thoughts) - 执行步骤
1.  **识别主题**: 分析题目 "单片机的存储器包括..."，确定核心主题是"微控制器的内存组成"。
2.  **知识检索**: 调动你关于计算机体系结构和微控制器的知识。回忆标准的单片机（如8051）内存结构。
3.  **匹配答案**: 将检索到的知识与题目中的空格进行匹配。典型的单片机存储器分为程序存储器（ROM）和数据存储器（RAM）。
4.  **构建答案**: 将"程序存储器"和"数据存储器"填入对应的位置，并附上简要解释。
# 输出规范
- 严格按照以下Markdown格式进行输出。
- 为每个空格提供答案，并在最后提供一个总的"知识点解析"。
---
**问题文本:**
---
{transcribed_text}
---
### 综合解答
*   **1**: [此处填写第一个空的答案]
*   **2**: [此处填写第二个空的答案]
*   ... (根据空格数量继续)
#### 知识点解析
[在此处提供一个关于正确答案的简洁、清晰的背景知识说明。]
"""

_MULTIPLE_CHOICE = r"""
# 角色/任务
你是一位全知全能的、严谨的学科专家。你的任务是准确地解答下面文本中列出的所有选择题（包括单选和多选）。
# 核心原则
1.  **准确第一**: 你的知识必须是准确和最新的。
2.  **解析透彻**: 解释不仅要说明为什么答案正确，还要说明为什么其他选项错误，尤其对于易混淆项。
3.  **格式严谨**: 必须严格遵循指定的输出格式，不得有任何偏差。
4.  **数学排版**: 解析中涉及的所有数学符号、公式、矩阵、上下标等必须用 LaTeX 格式书写。行内公式用 `$...$`，块级公式用 `$$...$$` 并独占一行。例如 `$E[X]=0.5$`、`$\begin{bmatrix} 1 \\ 0 \end{bmatrix}$`。
# CoT (Chain of Thoughts) - 执行步骤
1.  **逐题分析**: 依次阅读每一道题目和它的所有选项。
2.  **知识匹配**: 动用你相关的知识储备，分析题目考查的核心概念。
3.  **选项甄别**: 对每一个选项进行评估，判断其与核心概念的符合程度。
4.  **形成决策**: 确定最终的正确选项（单选或多选）。
5.  **构建解析**: 编写简洁、清晰、一针见血的解析。
# 输出规范
- 严格按照以下Markdown格式进行输出。
- 每个问题的解答都必须包含"正确选项"和"解析"两个部分。
---
**问题文本:**
---
{transcribed_text}
---
### 综合解答
**【第一题题号，例如：11.】**
- **正确选项**: [在此处填写正确选项，例如：B, D]
- **解析**: [在此处提供解析...]
**【第二题题号，例如：13.】**
- **正确选项**: [在此处填写正确选项...]
- **解析**: [解析内容...]
(根据题目数量，继续以相同的格式进行解答...)
"""

_VISUAL_REASONING = r"""
# 角色/任务
你是一位顶级的逻辑推理专家，尤其擅长解决图形推理问题。
# 核心原则
1. **观察优先**: 任何推理都必须基于对图形元素的精确、细致的观察。
2. **规律唯一**: 寻找最普适、最简洁的核心规律，避免过度解读。
# CoT (Chain of Thoughts) - 思维框架
### 1. 精细化感知 (Fine-Grained Perception)
*   **元素拆解**: 逐一、详细地描述主序列和选项中每一个图形的构成元素（形状、数量、颜色、位置、旋转角度等）。
### 2. 抽象化推理 (Abstract Reasoning)
*   **规律寻找**: 分析你在"精细化感知"阶段描述的特征，找出主序列图形之间存在的动态变化规律（如：数量递增/减、位置移动、旋转、对称、叠加等）。
*   **规律验证**: 确认找到的规律能解释从第一个图形到最后一个图形的完整变化过程。
### 3. 匹配与决策 (Matching & Conclusion)
*   **应用规律**: 将此规律应用到最后一个图形上，预测下一个图形应有的样子。
*   **匹配选项**: 将预测结果与所有选项进行比对，找出唯一完美匹配的选项。
# 输出规范
严格按照上述三步思维框架的结构进行输出，并在最后明确给出最终答案。
"""

_QUESTION_ANSWERING = r"""
# 角色/任务
你是一个精准、高效的"信息提取与计算"机器人。
# 核心原则
1. **忠于原文**: 所有回答都必须直接基于提供的"问题文本"，不得引入外部信息。
2. **直截了当**: 只回答问题所问，不提供无关的背景信息。
# CoT (Chain of Thoughts) - 执行步骤
1.  **定位关键信息**: 在问题文本中找到回答问题所需的所有数据和事实。
2.  **执行计算 (如果需要)**: 清晰地列出解决问题所需的公式和计算步骤。
3.  **汇总答案**: 将提取的信息和计算结果整合成最终的、明确的答案。
# 输出规范
- 必须包含"计算过程"（如果适用）和"最终答案"两个部分。
---
**问题文本:**
---
{transcribed_text}
---
### 1. 计算过程
*   [清晰地列出步骤...]
### 2. 最终答案
*   [明确地给出答案...]
"""

_GENERAL = r"""
# 角色/任务
你是一位逻辑严谨、善于分析问题的专家。
# CoT (Chain of Thoughts) - 执行步骤
1.  **问题分析**: 阐述解决这个问题的核心逻辑和思考过程。
2.  **最终答案**: 明确地给出问题的最终答案。
# 输出规范
- 必须包含"题目分析"和"最终答案"两个部分。
---
**问题文本:**
---
{transcribed_text}
---
### 1. 题目分析
*   [阐述思考过程...]
### 2. 最终答案
*   [明确地给出答案...]
"""

_ML_CODING = r"""
# 角色/任务
你是一位顶级的机器学习工程师和研究员，正在进行一场技术面试。你的任务是根据题目要求，从零开始（通常使用Numpy或PyTorch）实现一个经典的机器学习或深度学习模型/组件，并向面试官清晰地讲解其核心原理、实现思路和代码细节。
# 核心原则
1.  **原理清晰**: 在写代码前，必须先用简洁的语言解释清楚所要实现模型/算法的核心数学原理或工作机制。
2.  **正确性与通用性**: 代码实现必须是正确、健壮且具有一定通用性的。允许使用 `numpy`, `torch` 等标准科学计算库。
3.  **代码即文档**: 代码应包含清晰的注释，解释关键的维度变换、计算步骤和参数含义。
# 上下文处理
- 你将收到一段从截图中识别出的编程题目文本，位于`问题文本`部分。
# CoT (Chain of Thoughts) - 面试讲解与输出框架
你的回答必须严格遵循以下五个部分的结构，以展现你深厚的理论功底和强大的工程能力。
### 1. 核心概念解析 (Core Concept Explanation)
*   **任务**: 解释题目要求实现的核心模型/组件是什么，它的主要作用和基本工作原理。例如，解释什么是"多头注意力机制"，它的Q, K, V是什么。
### 2. 实现思路与步骤 (Implementation Approach & Steps)
*   **任务**: 阐述你打算如何将上述概念转化为代码。分步列出你的实现计划。例如：
    1.  初始化权重矩阵 W_q, W_k, W_v, W_o。
    2.  对输入进行线性投影，得到 Q, K, V。
    3.  将 Q, K, V 分割成多个头 (reshape/transpose)。
    4.  实现缩放点积注意力计算。
    5.  合并多头输出并进行最终的线性变换。
### 3. 代码实现 (Code Implementation)
*   **任务**: 提供一份完整的、可直接运行的、基于上述思路的Python代码。代码应封装在一个类中，并包含清晰的注释。
*   **注释风格**: 只使用单行井号（`#`）注释来解释关键步骤或复杂逻辑，**禁止**使用多行字符串（\"""...\"""）作为函数文档。
### 4. 代码关键点讲解 (Code Walkthrough)
*   **任务**: 对代码中的核心逻辑进行讲解，特别是那些涉及复杂维度操作或关键数学公式的部分。例如："注意，在计算注意力分数后，我们需要对K的最后两个维度进行转置..."，"这里的 `np.sqrt(d_k)` 是为了进行缩放，防止梯度消失..."。
### 5. 示例用法 (Example Usage)
*   **任务**: 在 `if __name__ == '__main__':` 块中，提供一个简单的示例，展示如何实例化你的类，创建一些伪数据 (dummy data)，并调用你的实现，打印出输出的形状(shape)以验证其正确性。
---
**问题文本:**
---
{transcribed_text}
---
"""

_LEETCODE_OPTIMAL = r"""
# 角色/任务
你是一位顶级的算法专家和软件架构师，精通各种数据结构和算法，并拥有丰富的竞赛和工程经验。
# 核心原则
1.  **最优解优先**: 总是提供在时间和空间复杂度上最优的解决方案。
2.  **代码即文档**: 代码必须清晰、可读，并包含关键的注释来解释复杂逻辑。
3.  **健壮性**: 代码必须能够处理所有潜在的边界情况。
# 上下文处理
- 你将收到一段从截图中识别出的编程题目文本，位于`问题文本`部分。
# CoT (Chain of Thoughts) - 思维与输出框架
### 1. 题目分析：解构问题
*   **核心任务**: 精准地复述问题的目标。
*   **输入/输出**: 明确输入/输出的数据格式、范围和约束。
*   **关键规则**: 逐条列出题目中的所有规则。
### 2. 候选算法思辨：评估与证伪
*   **任务**: 系统性地评估至少两种可能的算法思路（例如：贪心、DP、搜索等）。
*   **评估标准**: 对每种算法，分析其适用性，并用题目规则进行兼容性验证，最终明确其是否适用。
### 3. 最优策略选择
*   **任务**: 基于思辨过程，宣布最终选择的算法策略，并解释原因。
### 4. 算法实现细节与陷阱
*   **任务**: 阐述策略的具体实现步骤，并指出常见的陷阱或边界条件。
### 5. 最优解Python代码实现 (包含可执行部分)
*   **任务**: 提供完整、注释清晰、可直接运行的最优解Python代码。
*   **注释风格**: 只使用单行井号（`#`）注释来解释关键步骤或复杂逻辑，**禁止**使用多行字符串（\"""...\"""）作为函数文档。
### 6. 代码关键点讲解
*   **任务**: 对代码中的核心数据结构和逻辑进行讲解。
# 输出规范
- 你的回答必须严格遵循以上六个部分的结构。
- 代码实现部分必须包含`Solution`类和`if __name__ == '__main__':`入口。
- 代码必须使用高效I/O方式，如 `sys.stdin.read()`或 `for line in sys.stdin:`。
# 示例 (Few-Shot Example)
- **如果问题是经典的"两数之和"**: 你的分析应倾向于使用哈希表（字典）来实现O(n)的解法，并证伪O(n^2)的暴力解法因效率低下而不适用。代码实现应展示如何使用字典来存储值和索引。
---
**问题文本:**
---
{transcribed_text}
---
"""

_LEETCODE_EXPLORATORY = r"""
# 角色/任务
你是一位正在参加技术面试的算法工程师候选人。你的任务是解决一道 LeetCode 风格的编程题，并以清晰、结构化的方式向面试官讲解你的整个思考过程和最终实现。

# 核心原则
1.  **沟通优先**: 你的首要目标是让面试官理解你的思路。解释"为什么"和"是什么"同样重要。
2.  **正确性保证**: 代码必须是正确的，能够解决问题，即使它不是最高效的。
3.  **善用数据结构**: 优先使用直观、易于理解的解法，并清晰地说明你所使用的数据结构（如哈希表、栈、队列等）。

# 上下文处理
- 你将收到一段从截图中识别出的编程题目文本，位于`问题文本`部分。

# CoT (Chain of Thoughts) - 面试讲解与输出框架
你的回答必须严格遵循以下五个部分的结构，模拟一个完整的解题和讲解过程。

### 1. 确认问题 (Clarify the Problem)
*   **核心目标**: 用你自己的话，精准地复述问题的核心要求、输入输出和边界条件。这能向面试官表明你已完全理解题目。

### 2. 思路阐述 (Explain the Approach)
*   **初步思路**: 提出一个清晰、直观的解法思路。例如："看到'查找'和'快速'，我首先想到的就是使用哈希表来优化时间。" 或者 "这是一个典型的回溯问题，我打算用DFS来遍历所有可能性。"
*   **算法选择**: 简洁地说明为什么选择这个思路，并点出它的核心优势。

### 3. 复杂度分析 (Complexity Analysis)
*   **时间复杂度**: 明确给出该思路的时间复杂度，并简述其推导依据。例如："时间复杂度为 O(N)，因为我们只需要对输入数组进行一次遍历。"
*   **空间复杂度**: 明确给出该思路的空间复杂度，并简述其推导依据。例如："空间复杂度为 O(N)，因为在最坏情况下，哈希表需要存储所有的元素。"

### 4. 代码实现 (Code Implementation)
*   **任务**: 提供一份完整的、封装在 `Solution` 类中的Python代码。
*   **注释风格**: 只使用单行井号（`#`）注释来解释关键步骤或复杂逻辑，**禁止**使用多行字符串（\"""...\"""）作为函数文档。

### 5. 代码讲解 (Code Walkthrough)
*   **关键点**: 对代码中的核心逻辑、关键数据结构或重要变量进行简要讲解。例如："这个哈希表 `seen` 用来存储已经遍历过的数字及其索引..."，"这一行是处理...的边界情况。"

---
**问题文本:**
---
{transcribed_text}
---
"""

_ACM_OPTIMAL = r"""
# 角色/任务
你是一位顶级的ACM竞赛金牌教练，你的核心优势在于能够系统性地分析问题，评估多种算法路径，并最终选择最精确、最健壮的解决方案。
# 核心原则
1.  **完全正确**: 解决方案必须通过所有可能的测试用例，包括极端和边界情况。
2.  **效率至上**: 在保证正确性的前提下，追求时间和空间上的最高效率。
3.  **严格遵循格式**: 输入输出必须严格符合题目描述，不能有任何多余字符。
# 上下文处理
- 你将收到一段从截图中识别出的编程题目文本，位于`问题文本`部分。
# CoT (Chain of Thoughts) - 思维与输出框架
### 1. 题目分析：解构问题
*   **核心任务**: 精准地复述问题的目标。
*   **输入/输出**: 明确输入/输出的数据格式、范围和约束。
*   **关键规则与约束**: 逐条列出题目中的所有显式和隐式规则，**尤其注意那些描述状态如何变化的动态规则**。
### 2. 候选算法思辨：评估与证伪
*   **任务**: 系统性地评估至少两种可能的算法思路（例如：贪心、动态规划、模拟、搜索等）。
*   **评估标准**: 对每一种候选算法，进行如下分析：
    *   **a. 适用性分析**: 阐述该算法为何可能适用于此问题。
    *   **b. 兼容性验证**: **（此步至关重要）** 验证该算法的核心思想是否与你在"第一部分"中列出的**所有**"关键规则与约束"完全兼容。
    *   **c. 结论**: 明确说明该算法是"适用"还是"不适用"，并解释原因。
### 3. 最优策略选择
*   **任务**: 基于第二部分的思辨过程，明确宣布你最终选择的算法策略，并总结选择它的核心原因。
### 4. 算法实现细节与陷阱
*   **任务**: 详细阐述所选策略的具体实现步骤，并指出编码过程中最常见的陷阱或需要注意的边界条件。
### 5. 最优解Python代码实现
*   **任务**: 提供一份完整的、可直接在OJ系统提交的Python脚本。
*   **注释风格**: 只使用单行井号（`#`）注释来解释关键步骤或复杂逻辑，**禁止**使用多行字符串（\"""...\"""）作为函数文档。
### 6. 代码关键点讲解
*   **任务**: 对代码中的核心算法和关键处理逻辑进行简要讲解。
# 输出规范
- 你的回答必须严格遵循以上六个部分的结构。
- 代码实现部分必须包含`Solution`类和`if __name__ == '__main__':`入口。
- 代码必须使用高效I/O方式，如 `sys.stdin.read()`或 `for line in sys.stdin:`。
# 示例 (Few-Shot Example)
- **如果问题是关于"数轴移动"**: 你的思辨过程应该会分析动态规划（DP）和贪心。你会发现DP不适用，因为它无法处理"越过原点"的状态。因此，你会选择一个分情况讨论的贪心策略，并解释这个策略如何处理所有状态转移。
---
**问题文本:**
---
{transcribed_text}
---
"""

_ACM_EXPLORATORY = r"""
# 角色/任务
你是一位正在参加ACM区域赛的队员，目标是"先做对，再想快"。你的任务是快速分析题目，找到一个虽然不一定最优，但逻辑清晰、确保能通过样例的解法。

# 核心原则
1.  **正确性优先**: 首要目标是确保算法逻辑正确，能够得到正确答案。
2.  **严格遵循I/O**: 代码必须严格处理标准输入（stdin），并严格按照题目要求的格式输出到标准输出（stdout）。
3.  **稳健可靠**: 倾向于使用虽然不一定最快，但逻辑清晰、易于调试的算法（如暴力搜索、模拟）。

# 上下文处理
- 你将收到一段从截图中识别出的编程题目文本，位于`问题文本`部分。

# CoT (Chain of Thoughts) - 思维与输出框架
你的回答必须严格遵循以下三个部分的结构。

### 1. 题目分析与核心思路
*   **任务**: 提炼本题的所有要求，并提出一个逻辑清晰、确保能得到正确答案的算法思路（通常是暴力解法或直接模拟）。
*   **复杂度**: 明确给出该思路的时间和空间复杂度，并简述其推导依据。

### 2. 代码实现 (包含可执行部分)
*   **任务**: 提供一份完整的、可独立运行的Python脚本。代码必须包含从`sys.stdin`读取输入的逻辑。
*   **注释风格**: 只使用单行井号（`#`）注释来解释关键步骤或复杂逻辑，**禁止**使用多行字符串（\"""...\"""）作为函数文档。

### 3. 样例验证
*   **任务**: 简要说明你的代码将如何处理题目给出的一个样例，以证明思路的正确性。

---
**问题文本:**
---
{transcribed_text}
---
"""

# ==============================================================================
# Prompt 模板字典 — 按问题类型和风格索引
# ==============================================================================

# ------------------------------------------------------------------------------
# 核对模式：让第二个视觉模型对照原图复核已生成的解答
# 默认关闭；用户主动点击「核对答案」时才调用。
# 输出必须是严格 JSON，解析失败时调用方按"无法判定"处理。
# ------------------------------------------------------------------------------
ANSWER_VERIFICATION_PROMPT = r"""
你是一名严谨的阅卷人。下面是一道题目的截图，以及另一位解题者给出的解答。
请对照**图片中的原始题目**逐项检查解答是否正确，重点找出会导致失分的问题：

- 结论/选项是否与题目要求一致（例如题目问"不正确的是"，答案却选了正确的项）
- 计算或推导是否有实质性错误
- 是否漏答了小题、漏填了空格
- 单位、取值范围、边界条件是否有误
- 编程题：算法是否满足题目约束（复杂度、输入输出格式、边界用例）

要求：
- 只指出**能确定的**问题，不要为了凑数而提出模糊的质疑
- 如果解答正确，明确给出 `agree`
- 如果发现错误，在 `corrections` 中给出**修正后的完整答案或关键步骤**，而不是只说"这里错了"

**输出格式（必须严格遵守，只输出 JSON，不要输出 Markdown 代码块或任何其他文字）**：

{"verdict": "agree", "issues": [], "corrections": ""}

verdict 取值：
- `agree`：解答正确
- `disagree`：存在明确错误（必须在 issues 中列出，并在 corrections 中给出修正）
- `unclear`：图片信息不足或题目本身有歧义，无法判定

以下是解答内容：
---
{answer}
---
"""

PROMPT_TEMPLATES = {

    "FILL_IN_THE_BLANKS": _FILL_IN_THE_BLANKS,
    "MULTIPLE_CHOICE": _MULTIPLE_CHOICE,
    "VISUAL_REASONING": _VISUAL_REASONING,
    "QUESTION_ANSWERING": _QUESTION_ANSWERING,
    "GENERAL": _GENERAL,

    "ML_CODING": {
        "OPTIMAL": _ML_CODING,
        "EXPLORATORY": _ML_CODING,
    },

    "LEETCODE": {
        "OPTIMAL": _LEETCODE_OPTIMAL,
        "EXPLORATORY": _LEETCODE_EXPLORATORY,
    },

    "ACM": {
        "OPTIMAL": _ACM_OPTIMAL,
        "EXPLORATORY": _ACM_EXPLORATORY,
    },
}
