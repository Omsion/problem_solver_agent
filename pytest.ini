[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers
filterwarnings =
    # Starlette 的 TestClient 依赖 httpx 的告警与本项目无关
    ignore::DeprecationWarning:starlette.*
    ignore::DeprecationWarning:httpx.*
